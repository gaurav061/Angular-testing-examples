/**
 *
 * @param name{string} User name
 * @returns user name prefixed with 'Hello !!'
 * e.g: greet('user') will yeild Hello !! user
 */
export function greet(name: string) {
  return 'Hello !! ' + name;
}
